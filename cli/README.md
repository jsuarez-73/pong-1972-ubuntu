## [INSPECTOR]
>To change the port to inspect the program define the variable PORT to the desire one, follow by npm run inspect.

## [ANNOTATIONS]
>The PongCourt class is intended to connect with remote players, this is not intended to play with another player locally, the subject don't ask for that implementation.
>The terminal's screen size in 1972 was 24 x 80, the default size for virtual terminals nowdays, based on this we'll support sizes greater than this one but not less.

>Remember that CLI will be a service as well which is
intended to serves the shell scripts encharge to download the
whole project, thus it's possible during the building time of
the service, create the private and public, and also the
registration json and then all the client will share the same ones.

>[PENDING]: tools must be set inside the docker service, and must be set
dinamically in package.json.

