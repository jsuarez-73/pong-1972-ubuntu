{
	"name": "@segfaultx/sql",
	"version": "1.0.0",
	"description": "SegFaultx library SQL lite connections",
	"main": "dist/src/sqlite.js",
	"types": "dist/src/sqlite.d.ts",
	"scripts": {
		"build": "tsc",
		"postinstall": "npx tsc && npx tsc-alias -p tsconfig.json",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"@types/node": "^22.14.0",
		"tsc-alias": "^1.8.16",
		"typescript": "^5.8.3"
	},
	"author": "segfaultx",
	"license": "ISC",
	"description": "Lib for SQL lite conections"
}
